import { View } from '@tarojs/components'
import { FC } from '@tarojs/taro'
import React, { memo } from 'react'

const My: FC = memo(() => {

  return (
    <View>

    </View>
  )
})

export default My
